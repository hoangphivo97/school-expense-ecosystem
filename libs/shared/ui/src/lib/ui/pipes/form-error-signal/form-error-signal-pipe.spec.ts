import { FormErrorSignalPipe } from './form-error-signal-pipe';

describe('FormErrorSignalPipe', () => {
  it('create an instance', () => {
    const pipe = new FormErrorSignalPipe();
    expect(pipe).toBeTruthy();
  });
});
