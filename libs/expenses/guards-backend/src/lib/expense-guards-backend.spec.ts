import { expenseGuardsBackend } from './expense-guards-backend';

describe('expenseGuardsBackend', () => {
  it('should work', () => {
    expect(expenseGuardsBackend()).toEqual('expense-guards-backend');
  });
});
